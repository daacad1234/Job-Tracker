# Copy this file to .env and set a real secret before running docker compose
# anywhere beyond your own machine. Generate one with: openssl rand -base64 64
JWT_SECRET=change_me_to_a_long_random_value
